# Addres <127.0.0.1:8000/predict_test>
    NO BROWSER

# Postman <localhost:8000/predict>
    "features": "number",
    "label": "null"

# Build <sudo docker-compose build>

# Run <sudo docker-compose up>